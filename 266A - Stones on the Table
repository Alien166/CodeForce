#include <iostream>
using namespace std;
string b;
int n, m;
int main() 
{
    cin >> n >> b;
    for (int i = 0; i < n; i++) 
    {
        if (b[i] == b[i + 1]) 
        {
            m++;
        }
    }
    cout << m;
}
