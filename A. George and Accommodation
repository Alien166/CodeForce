#include<bits/stdc++.h>
using namespace std;
int main()
{
int n,peo,cap,sum=0;  cin>>n;
for(int i=0;i<n;i++)
{
    cin>>peo>>cap;
    if(cap-peo>=2) {
        sum++;
    }
}
    cout<<sum;
    return 0;
}
